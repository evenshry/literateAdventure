/**
 * API 服务层示例
 * 定义具体的 API 接口调用
 */

import type { ApiResponse } from './request';

// 示例：用户相关 API（当前项目使用本地存储，此文件为未来扩展预留）
export const userApi = {
  // 示例接口定义
  // login: (params: LoginParams) => request.post('/auth/login', params),
  // getUserInfo: () => request.get('/user/info'),
};

export default userApi;
